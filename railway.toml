[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "supervisord -c /etc/supervisor/conf.d/idea.conf"
healthcheckPath = "/api/health"
healthcheckTimeout = 30

[[services]]
name = "web"
